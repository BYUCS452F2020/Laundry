package models.result.user;

import models.result.Result;

public class RegisterResult extends Result {

    public RegisterResult(String theMessage, boolean theSuccess) {
        super(theMessage, theSuccess);
    }
}
